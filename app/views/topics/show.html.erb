
<div class="col-sm-8 blog-main">

  <h1><%= @topic.title %></h1>

	<%= paginate @blogs, theme: 'twitter-bootstrap-4', pagination_class: "pagination-sm" %>
	<%= render @blogs %>
	<%= paginate @blogs, theme: 'twitter-bootstrap-4', pagination_class: "pagination-sm" %>

</div>
